# HIG Doctor

This section contains curated Apple Human Interface Guidelines skills and reference files for iOS, iPadOS, macOS, tvOS, visionOS, and watchOS.

- Start here when you need structured Apple HIG guidance instead of raw Apple docs.
- The `hig-doctor` command is already available globally on this machine.
- Use `--expand` when you need the nested reference file paths instead of just the skill index.
