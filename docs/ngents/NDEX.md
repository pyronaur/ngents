# ngents Docs

This topic contains docs about the ngents toolkit itself, including the `ndex` command and docs authoring conventions.

- Start with `authoring.md` when you are changing how docs discovery or docs structure works.
- Keep these docs focused on current behavior and authoring rules.
