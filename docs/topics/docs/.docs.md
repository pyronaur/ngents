---
title: Docs CLI internals
short: Internal rendering and authoring references for the docs CLI itself.
summary: Reference material about how the docs CLI renders topics, collections, and docs lists. The canonical user-facing guide lives at the docs root in ngents-docs.md.
read_when:
  - Investigating a docs CLI rendering regression.
  - Need to understand expected output shapes for topic, collection, or docs-list views.
  - Authoring or reviewing a change to docs CLI render behavior.
---

# Docs CLI internals

Internal reference for the `docs` CLI render and authoring layer.

The user-facing guide for the `docs` command is at the root of this docs root.
Open it with `docs` or read `~/.ngents/docs/ngents-docs.md` directly.
